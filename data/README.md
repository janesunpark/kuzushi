# data

This directory contains data used to develop and test the Kuzushi data pipeline, including raw source files and generated medallion-layer snapshots.

Private data files are excluded from version control. Only documentation and placeholder files are tracked in Git. 
