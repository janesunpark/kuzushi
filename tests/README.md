## Testing

Bronze-layer ingestion and snapshot behavior are tested with `pytest`, including missing-file handling, schema preservation, Parquet round-trips, and rollback after partial write failures.


